// Package format provides a pluggable system for rendering data in various
// human-readable and machine-readable formats (JSON, YAML, Table, etc.).
// It abstracts the presentation layer from the CLI commands.
package format
