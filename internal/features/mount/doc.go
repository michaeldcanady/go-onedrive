// Package mount manages the mapping of logical VFS paths to specific storage
// providers and identities. It handles the persistence of these mount points,
// allowing the VFS to coordinate across multiple backends.
package mount
