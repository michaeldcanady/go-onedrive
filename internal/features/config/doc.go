// Package config manages hierarchical application settings using dot-notation keys
// (e.g., "identity.azure.client_id"). It supports persistent storage through
// interchangeable repositories and provides default values for core identity providers.
package config
