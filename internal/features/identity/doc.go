// Package identity manages the lifecycle of user profiles and authentication tokens.
// It supports interactive login via external identity plugins, proactive token
// refreshing, and persistent storage of authenticated identities.
package identity
