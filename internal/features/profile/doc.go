// Package profile manages user-specific application profiles and session state.
// It allows users to switch between different sets of configurations, identities,
// and mount points by managing a 'current' profile indicator.
package profile
