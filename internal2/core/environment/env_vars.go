package environment

const (
	// EnvLogLevel is the environment variable for the log level.
	EnvLogLevel = "ODC_LOG_LEVEL"
	// EnvLogOutput is the environment variable for the log output destination.
	EnvLogOutput = "ODC_LOG_OUTPUT"
	// EnvLogDir is the environment variable for the log directory.
	EnvLogDir = "ODC_LOG_PATH"
	// EnvTempDir is the environment variable for the temporary directory.
	EnvTempDir = "ODC_TEMP_DIR"
	// EnvCacheDir is the environment variable for the cache directory.
	EnvCacheDir = "ODC_CACHE_DIR"
	// EnvDataDir is the environment variable for the data directory.
	EnvDataDir = "ODC_DATA_DIR"
	// EnvConfigDir is the environment variable for the configuration directory.
	EnvConfigDir = "ODC_CONFIG_DIR"
	// EnvStateDir is the environment variable for the state directory.
	EnvStateDir = "ODC_STATE_DIR"
	// EnvShell is the environment variable for the shell.
	EnvShell = "ODC_SHELL"
	// EnvEditor is the environment variable for the editor.
	EnvEditor = "ODC_EDITOR"
	// EnvVisual is the environment variable for the visual editor.
	EnvVisual = "ODC_VISUAL"
)
