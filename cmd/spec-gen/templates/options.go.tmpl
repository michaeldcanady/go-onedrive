// Code generated by spec-gen. DO NOT EDIT.
package {{.Name}}

import (
	"io"
)

// Options encapsulates all user-provided arguments and flags for the command.
type Options struct {
	{{- range .Args }}
	{{.Name | pascal}} string // {{.Description}}
	{{- end }}

	{{- range .Flags }}
	{{- if eq .Type "string" }}
	{{.Name | pascal}} string // {{.Description}}
	{{- else if eq .Type "bool" }}
	{{.Name | pascal}} bool // {{.Description}}
	{{- else if eq .Type "stringSlice" }}
	{{.Name | pascal}} []string // {{.Description}}
	{{- end }}
	{{- end }}

	// Stdout receives standard output messages.
	Stdout io.Writer
	// Stderr receives error and diagnostic messages.
	Stderr io.Writer
}
